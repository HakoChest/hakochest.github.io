name: Deploy GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'latest'
          cache: 'npm'

      - name: Install dependencies and build Vue
        run: npm ci && npm run build

      - name: Setup mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: '0.4.10'
        
      - name: Install Mdbook Mermaid
        run: cargo install mdbook-mermaid
        
      - name: Build mdbooks
        run: |
          mdbook build gxci-book-cn
          mdbook build gxci-book-en
          mdbook build vielpork-book-cn
          mdbook build vielpork-book-en

      - name: Prepare combined output
        run: |
          mkdir -p combined-output
          cp -r dist/* combined-output/
          mkdir -p combined-output/{gxci-cn,gxci-en,vielpork-cn,vielpork-en}
          cp -r gxci-book-cn/book/* combined-output/gxci-cn/
          cp -r gxci-book-en/book/* combined-output/gxci-en/
          cp -r vielpork-book-cn/book/* combined-output/vielpork-cn/
          cp -r vielpork-book-en/book/* combined-output/vielpork-en/

      - name: Generate TypeDoc and deploy
        run: |
          npm install -g typedoc
          node .github/scripts/generate-typedoc.js

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./combined-output